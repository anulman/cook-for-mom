{{#section-info class=styleNamespace as |info|}}
  {{yield (hash
    info=info
    toc=(component tocComponent)
    section=(component sectionComponent)
    content=(component contentComponent)
  )}}
{{/section-info}}
