{{yield (hash
  titleComponent=(component titleComponent section.title)
  anchor=(component anchorComponent section.slug)
  title=(component titleComponent section.title)
  body=(component contentComponent)
  layoutFetcher=layoutFetcher
  raw=section
)}}
